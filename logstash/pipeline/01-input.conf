input {
  beats {
    port => 5044
    client_inactivity_timeout => 300
  }

  udp {
    port => 5000
    codec => plain { charset => "UTF-8" }
    tags => ["syslog_udp"]
  }

  tcp {
    port => 5001
    codec => json
    tags => ["json_tcp"]
  }
}
